import 'package:flutter/material.dart';

class UserNotice extends StatelessWidget {
  const UserNotice({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("User Notice");
  }
}
